import '/_imports.dart';

class SlidingDrawer extends StatelessWidget {
  const SlidingDrawer({super.key});
  @override
  Widget build (BuildContext context) {
    return const Text('Hello Collin');
  }
}